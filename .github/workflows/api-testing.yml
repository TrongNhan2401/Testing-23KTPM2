name: EShop API Automated Testing (HW06)

on:
  push:
    branches: [ "HW06", "main", "master" ]
  pull_request:
    branches: [ "HW06", "main", "master" ]

jobs:
  api-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository code
      uses: actions/checkout@v4

    - name: Set up Node.js environment
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install Dependencies
      run: |
        npm install -g newman newman-reporter-htmlextra
        cd eshop/backend
        npm install

    - name: Start EShop Server
      run: |
        cd eshop/backend
        nohup node server.js > server.log 2>&1 &
        sleep 5
        cat server.log
        curl -i http://localhost:3000/api/products || echo "Server check completed"

    - name: Run Newman API Tests
      run: |
        export NODE_PATH=$(npm root -g)
        npx newman run ./HW06/HW06_EShop_Collection.json -e ./HW06/EShop_Environment.json -r cli,htmlextra --reporter-htmlextra-export ./HW06/newman_report.html

    - name: Upload Newman HTML Test Report Artifact
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: newman-html-report
        path: ./HW06/newman_report.html
